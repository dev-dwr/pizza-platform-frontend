{
  "extends": "next/core-web-vitals",
  "rules": {
    "react-hooks/exhaustive-deps": "warn",  // Set hook dependency warnings to not fail the build
    "react/jsx-key": "warn"                // Set missing key warning to not fail the build
  }
}
